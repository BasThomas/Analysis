language: objective-c
install:
  - brew remove --force xctool
  - brew install xctool --HEAD

script:
  - xctool run-tests -workspace Example/Analysis.xcworkspace -scheme Analysis_Tests -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
